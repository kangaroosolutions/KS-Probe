models:
  # ── OpenAI ────────────────────────────────────────────────────────────────
  - id: gpt-5.2
    provider: openai
    max_context_tokens: 200000
    cost_per_1k_input: 0.0016
    cost_per_1k_output: 0.0048
    notes: "ChatGPT 5.2 — actual rates from OpenAI dashboard (Mar 2026)"

  # ── Anthropic ─────────────────────────────────────────────────────────────
  - id: claude-sonnet-4-6
    provider: anthropic
    max_context_tokens: 200000
    cost_per_1k_input: 0.003
    cost_per_1k_output: 0.015

  # ── xAI ───────────────────────────────────────────────────────────────────
  - id: grok-4.1-fast
    provider: xai
    max_context_tokens: 131072
    cost_per_1k_input: 0.00025
    cost_per_1k_output: 0.00075

  # ── DeepSeek ──────────────────────────────────────────────────────────────
  - id: deepseek-v3.2
    provider: deepseek
    max_context_tokens: 65536
    cost_per_1k_input: 0.00027
    cost_per_1k_output: 0.00110

  # ── Mock ──────────────────────────────────────────────────────────────────
  - id: mock
    provider: mock
    max_context_tokens: 1000000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    notes: "Deterministic mock — no API keys needed. Use for CI and local testing."
